
Importer Featurues: 

UI Style - Importer like node based workflow editor 

Cron Like Jobs
Email Sync, 
Verify txns using SMS/Emails 
TxNs get a tick when there are verified

It should have User Sessoins - some validations needed to add, so if current user seesoisns has the same transactions mark them as -> similar found, which is in progress
When having large csv imports convert them into multiple user sessoins. 
or better yet, first find very unique transactions, learn and then apply those categorisatiosn to rest. 
Or split big transctions imports into multiple user sessions

Input algos -> match against


