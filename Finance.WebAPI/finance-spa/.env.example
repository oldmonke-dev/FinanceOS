# Browser requests go through the Next.js proxy.
NEXT_PUBLIC_FINANCE_API_BASE_URL=/api-proxy

# Server-side requests and the proxy target the API directly.
FINANCE_API_SERVER_BASE_URL=http://localhost:5132
