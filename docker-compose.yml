services:
  postgres:
    image: postgres:17-alpine
    container_name: finance-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: finance_db
      POSTGRES_USER: finance_user
      POSTGRES_PASSWORD: strongpassword
    ports:
      - "5432:5432"
    volumes:
      - finance-postgres-data:/var/lib/postgresql/data
    networks:
      - finance-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U finance_user -d finance_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Finance.WebAPI/Dockerfile
      provenance: false
      sbom: false
    container_name: finance-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=finance_db;Username=finance_user;Password=strongpassword
      Cors__AllowedOrigins__0: http://localhost:3056
      Cors__AllowedOrigins__1: http://127.0.0.1:3056
    ports:
      - "5133:8080"
    networks:
      - finance-network

  spa:
    build:
      context: Finance.WebAPI/finance-spa
      dockerfile: Dockerfile
      provenance: false
      sbom: false
      args:
        NEXT_PUBLIC_FINANCE_API_BASE_URL: http://localhost:5133
    container_name: finance-spa
    restart: unless-stopped
    depends_on:
      - api
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_FINANCE_API_BASE_URL: http://localhost:5133
    ports:
      - "3056:3000"
    networks:
      - finance-network

volumes:
  finance-postgres-data:

networks:
  finance-network:
    name: finance-network
    driver: bridge
